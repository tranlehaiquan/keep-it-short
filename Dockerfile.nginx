FROM nginx:alpine

# Copy nginx config
COPY nginx/nginx.conf /etc/nginx/conf.d/app.conf

# Static files mounted via volume in docker-compose
# Volume: web_dist:/usr/share/nginx/html

EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
