server {
    listen 80;
    server_name tranlehaiquan.id.vn;

    # Proxy API requests
    location /api/ {
        proxy_pass http://api:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Short-link redirects (nanoid slugs, 6 chars).
    location ~ "^/[0-9A-Za-z_-]{6}$" {
        proxy_pass http://api:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
    }

    # Everything else -> web (port 4000)
    location / {
        proxy_pass http://web:4000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# -----------------------------------------------------------------------------
# Stage 2: HTTPS configuration (uncomment and replace stage 1 after issuing certs)
# Note: leave the /.well-known/acme-challenge location present on port 80 to
# allow renewals via certbot; the server block below redirects HTTP->HTTPS.
# To enable stage 2, replace the stage-1 server block above with the two blocks
# below (or update your Docker image to use this file with stage-2 active).
# -----------------------------------------------------------------------------

#server {
#    listen 80;
#    server_name tranlehaiquan.id.vn;
#
#    location /.well-known/acme-challenge/ {
#        root /var/www/certbot;
#    }
#
#    # Redirect all other traffic to HTTPS
#    location / {
#        return 301 https://$host$request_uri;
#    }
#}
#
#server {
#    listen 443 ssl http2;
#    server_name tranlehaiquan.id.vn;
#
#    ssl_certificate /etc/letsencrypt/live/tranlehaiquan.id.vn/fullchain.pem;
#    ssl_certificate_key /etc/letsencrypt/live/tranlehaiquan.id.vn/privkey.pem;
#    ssl_protocols TLSv1.2 TLSv1.3;
#    ssl_prefer_server_ciphers on;
#    ssl_ciphers "EECDH+AESGCM:EDH+AESGCM:AES256+EECDH:AES256+EDH";
#    ssl_session_cache shared:SSL:10m;
#    ssl_session_timeout 10m;
#
#    # Proxy API requests
#    location /api/ {
#        proxy_pass http://api:3000;
#        proxy_http_version 1.1;
#        proxy_set_header Host $host;
#        proxy_set_header X-Real-IP $remote_addr;
#        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#        proxy_set_header X-Forwarded-Proto https;
#    }
#
#    location ~ "^/[0-9A-Za-z_-]{6}$" {
#        proxy_pass http://api:3000;
#        proxy_http_version 1.1;
#        proxy_set_header Host $host;
#    }
#
#    location / {
#        proxy_pass http://web:4000;
#        proxy_http_version 1.1;
#        proxy_set_header Host $host;
#        proxy_set_header X-Real-IP $remote_addr;
#        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#        proxy_set_header X-Forwarded-Proto https;
#    }
#}
